{
  "name": "laced-baddies-site",
  "version": "1.0.0",
  "private": true,
  "description": "Laced Baddies product landing page served with Node.js.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check api/swell-config.js && node --check lib/swell-config.js && node --check ecommerce.js"
  },
  "engines": {
    "node": ">=18"
  }
}
