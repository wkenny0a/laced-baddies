# Laced Baddies Logo Assets

Use `laced-baddies-social-logo.png` for Facebook and Instagram profile images.

Use `laced-baddies-wordmark.svg` for horizontal website/header use.

Use `favicon.ico`, `favicon.svg`, `favicon-32.png`, and `apple-touch-icon.png` for browser/app icons.
